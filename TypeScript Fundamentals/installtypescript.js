I found instructions to install globally, but not sure if it.

not sure if i missed a

need JSON file first in project?

run init?

let age: Number;

age = 12;

let userName: String;

userName = 'Max';

let isInstructor: Boolean;

isInstructor = true;